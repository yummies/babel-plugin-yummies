{
  "name": "@yummies/babel-plugin-yummies",
  "version": "0.5.0-beta.1",
  "description": "Multilayer components inheritance for Yummies via Babel plugin",
  "keywords": [ "yummies", "babel", "babel-plugin" ],
  "homepage": "https://github.com/yummies/babel-plugin-yummies",
  "repository": "yummies/babel-plugin-yummies",
  "contributors": [
    "Kir Belevich <kir@soulshine.in> (https://github.com/deepsweet)",
    "Denis Koltsov <iam@mistadikay.com> (https://github.com/mistadikay)"
  ],
  "main": "build/index.js",
  "files": [
    "build/",
    "LICENSE"
  ],
  "dependencies": {
    "babel-runtime": "5.8.x",
    "loader-utils": "0.2.x",
    "js-yaml": "3.3.x"
  },
  "devDependencies": {
    "husky": "0.9.x",
    "mocha": "2.2.x",
    "chai": "3.2.x",
    "isparta": "3.0.x",
    "coveralls": "2.11.x",
    "eslint": "1.0.x",
    "babel-eslint": "4.0.x",
    "eslint-plugin-babel": "2.0.x",
    "babel": "5.8.x",
    "babel-core": "5.8.x",
    "chokidar-cli": "1.0.x"
  },
  "scripts": {
    "build": "rm -rf build/ && babel lib/ -d build/",
    "dev": "npm run build -- -w",
    "lint": "eslint --ext .es6 lib/ test/",

    "coverage": "rm -rf coverage/ && isparta cover _mocha -i lib/*.es6",
    "coverage-dev": "chokidar 'lib/*.es6' 'test/lib/*.es6' -c 'npm run coverage' --initial",
    "test": "npm run lint && npm run coverage",
    "travis": "npm run test && cat coverage/lcov.info | coveralls",

    "prepush": "npm run test",
    "prepublish": "npm run build"
  },
  "engines": {
    "node": ">=0.12.0",
    "npm": ">=2.7.0"
  },
  "license": "MIT"
}
